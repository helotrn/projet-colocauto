<?php

namespace App\Transformers;

class UserCommunityTransformer extends BaseTransformer
{
    public function transform($item, $options = []) {
        $output = parent::transform($item, $options);

        return $output;
    }
}
